import React from 'react'
    //Navbar with account address
    class Main extends React.Component{
        render() {
            return(
                <span>

                </span>
            );

        }
    }

export default Main
